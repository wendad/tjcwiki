sendme-发送当前页面ID号到串口
===============================================================

sendme

实例1：sendme   (此指令不需要参数)

备注:

设备收到此指令会立刻把当前页面的ID号发送到串口，如果想要每次刷新页面自动发送页面ID,请在页面的初始化事件里写上sendme语句即可。发送格式请参看本表格后面的”串口HMI设备返回数据格式”表格。
