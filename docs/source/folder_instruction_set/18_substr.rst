substr-字符串截取
===============================================================

substr att0,att1,star,lenth

att0:源变量(必须是字符串变量)

att1:目标变量(必须是字符串变量)

star:在源变量中的字符起始位置

lenth:截取字符串长度

实例:substr t0.txt,t1.txt,0,2  (从t0.txt中的0位置开始截取2个字符赋值给t1.txt)